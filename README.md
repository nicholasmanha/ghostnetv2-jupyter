
Setup guide:  
`conda create -n ghostnet python=3.9`  
`conda activate ghostnet`  
`pip install -r requirements.txt`  

Making a jupyter kernel with the environment:  
`python -m ipykernel install --user --name=ghostnet --display-name "Python ghostnet"`  

When running jupyter notebook, change the kernel to "Python ghostnet"
